# 축제 데이터 API 가이드

## 🚨 위치기반서비스사업신고필증 문제

한국관광공사 API는 **위치기반서비스사업신고필증**이 필요하여 사용이 어렵습니다. 대신 아래 방법들을 사용할 수 있습니다.

## ✅ 추천 방법: 문화체육관광부 API

### 1. 공공데이터포털에서 API 신청

1. [공공데이터포털](https://www.data.go.kr/) 접속
2. "문화체육관광부" 검색
3. 다음 API 중 하나 선택:
   - **문화체육관광부_지역축제정보**
   - **문화체육관광부_축제행사정보**
   - **문화체육관광부_관광축제정보**

4. 활용신청 후 인증키 발급
5. GitHub Secrets에 `DATA_GO_KR_KEY`로 저장

### 2. API 엔드포인트 확인

공공데이터포털에서 신청한 API의 상세 페이지에서:
- API 엔드포인트 URL 확인
- 필수 파라미터 확인
- 응답 형식 확인

### 3. 스크립트 수정

`scripts/updateFestivals.js` 파일의 `fetchFromMCSTAPI()` 함수에서:
- API_URL을 실제 엔드포인트로 수정
- 파라미터를 API 문서에 맞게 수정
- 응답 데이터 파싱 로직 수정

## 🔄 대안 방법들

### 방법 1: 웹 스크래핑

각 지자체나 관광 사이트에서 직접 데이터 수집:

```javascript
// 예시: 한국관광공사 축제 페이지
// https://korean.visitkorea.or.kr/list/fes/list.do

// 필요한 라이브러리
npm install puppeteer cheerio
```

**장점:**
- 위치기반서비스사업신고필증 불필요
- 다양한 데이터 소스 활용 가능

**단점:**
- 웹사이트 구조 변경 시 수정 필요
- 이용약관 확인 필요
- 서버 부하 고려 필요

### 방법 2: 각 지자체별 API

일부 지자체는 자체 API를 제공합니다:

- 서울시: [서울열린데이터광장](https://data.seoul.go.kr/)
- 부산시: [부산광역시 공공데이터포털](https://data.busan.go.kr/)
- 제주도: [제주특별자치도 공공데이터포털](https://www.jeju.go.kr/)

**장점:**
- 지역별 상세 정보
- 위치기반서비스사업신고필증 불필요 (대부분)

**단점:**
- 지자체별로 API 형식이 다름
- 여러 API를 통합해야 함

### 방법 3: RSS 피드 활용

일부 관광 사이트는 RSS 피드를 제공합니다:

```javascript
// RSS 피드 파싱
const parser = require('rss-parser');
const feed = await parser.parseURL('https://example.com/festivals/rss');
```

### 방법 4: 수동 업데이트 + 자동화

1. 수동으로 주요 축제 정보 수집
2. JSON 파일로 관리
3. GitHub Actions로 자동 커밋/배포

## 📝 현재 스크립트 설정

현재 `scripts/updateFestivals.js`는:

1. **문화체육관광부 API** 우선 시도
2. 실패 시 **웹 스크래핑** 시도
3. 둘 다 실패 시 기존 데이터 유지

## 🔧 API 키 설정

GitHub Secrets에 추가:
- Name: `DATA_GO_KR_KEY`
- Value: 공공데이터포털에서 발급받은 인증키

## 📚 참고 자료

- [공공데이터포털](https://www.data.go.kr/)
- [문화체육관광부](https://www.mcst.go.kr/)
- [한국관광공사](https://kto.visitkorea.or.kr/)

## ⚠️ 주의사항

1. **이용약관 확인**: 웹 스크래핑 시 해당 사이트의 이용약관 확인
2. **요청 빈도**: API 호출이나 스크래핑 시 적절한 딜레이 추가
3. **에러 처리**: API 실패 시 기존 데이터 유지하도록 구현
4. **데이터 검증**: 수집한 데이터의 유효성 검증 필요
